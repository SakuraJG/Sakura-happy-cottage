# 安全政策

## 支持范围

在首个稳定版本发布前，仅默认分支 `main` 接收安全修复。发布稳定版本后，应在此文件维护仍受支持的版本范围。

## 报告漏洞

请使用仓库 `Security` 页面中的 **Report a vulnerability** 创建私有安全公告草稿。不要创建公开 Issue、Discussion 或 Pull Request，也不要在报告中包含不必要的真实用户数据。

报告应包含：

- 受影响的提交或版本；
- 可复现的最小步骤或概念验证；
- 实际影响和所需前置条件；
- 已知的缓解方式。

维护者应在 3 个工作日内确认收到，在完成影响评估后协调修复与披露时间。修复发布前，请不要公开漏洞细节。

## 部署责任

生产部署必须使用 HTTPS、安全 Cookie、强且唯一的管理员及数据库密码，并限制 PostgreSQL 和上传目录的网络与文件权限。示例配置只用于说明字段，不能直接作为公网生产配置。
